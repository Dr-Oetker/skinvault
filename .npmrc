# Handle optional dependency issues
optional=false
# Disable platform-specific binaries that cause issues
platform=linux
arch=x64
# Ignore scripts to prevent platform-specific binary installation
ignore-scripts=true 